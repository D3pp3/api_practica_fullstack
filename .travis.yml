language: java
branches:
  only:
    - main
notifications:
  email:
    - joalvarez.depp3@gmail.com
install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
deploy:
  provider: heroku
  api_key: ${HEROKU}
  on:
    branch: main